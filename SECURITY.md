# Security

Report vulnerabilities privately. Do not open public issues for secrets or pairing tokens.

This client stores the device private key locally (Windows DPAPI). Pairing tokens must never be logged or placed on argv.
